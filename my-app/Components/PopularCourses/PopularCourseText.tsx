import React from 'react'

type Props = {
    PopularTitle: string,
    PopularDescription: string,
}

const PopularText = ({PopularTitle, PopularDescription}: Props) => {
  return (
    <div>PopularText</div>
  )
}